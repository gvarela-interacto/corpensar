from django.apps import AppConfig

class TemplatetagsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'corpensar.templatetags'  # Reemplaza 'tu_app' con el nombre de tu aplicación